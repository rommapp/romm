[mypy]
check_untyped_defs = True
disable_error_code = import-untyped
# A regex, not a TOML list: an array here compiles to a character class that
# matches almost every path, excluding the whole backend.
exclude = (?x)(^|/)alembic/
